<!DOCTYPE html>
<html lang="ku" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Business Links</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-bg: #0f172a;
            --card-bg: rgba(255, 255, 255, 0.05);
            --text-color: #ffffff;
        }
        body {
            background: radial-gradient(circle at top, #1e293b, #0f172a);
            color: var(--text-color);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 40px 20px;
            margin: 0;
            min-height: 100vh;
        }
        .profile-container { text-align: center; margin-bottom: 30px; }
        .profile-img {
            width: 110px; height: 110px;
            border-radius: 50%;
            border: 4px solid #3b82f6;
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
            object-fit: cover;
        }
        h1 { font-size: 24px; margin: 10px 0 5px; letter-spacing: 1px; }
        .bio { color: #94a3b8; font-size: 14px; margin-bottom: 20px; }

        .links-container { width: 100%; max-width: 400px; display: flex; flex-direction: column; gap: 15px; }
        
        .btn {
            display: flex; align-items: center; justify-content: space-between;
            padding: 16px 20px; border-radius: 16px;
            text-decoration: none; color: white; font-weight: 600;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        .btn:hover { transform: translateY(-3px); box-shadow: 0 10px 20px rgba(0,0,0,0.3); }
        .btn i { font-size: 20px; }

        /* ڕەنگی تایبەت بە هەر دوگمەیەک */
        .whatsapp { background: linear-gradient(90deg, #25d366, #128c7e); }
        .telegram { background: linear-gradient(90deg, #0088cc, #0077b5); }
        .instagram { background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888); }
        .snapchat { background: linear-gradient(90deg, #fffc00, #f7f400); color: black; }
        .viber { background: linear-gradient(90deg, #7360f2, #503ebd); }
        .facebook { background: linear-gradient(90deg, #1877f2, #0e5a8a); }

        footer { margin-top: auto; padding-top: 40px; font-size: 12px; color: #64748b; }
    </style>
</head>
<body>

    <div class="profile-container">
        <img src="https://via.placeholder.com/150" alt="Profile" class="profile-img">
        <h1>ناوی کڕیار لێرە بنووسە</h1>
        <p class="bio">Marketing & Sales Specialist</p>
    </div>

    <div class="links-container">
        <a href="https://wa.me/964750xxxxxxx" class="btn whatsapp">
            <i class="fab fa-whatsapp"></i> <span>WhatsApp</span> <div></div>
        </a>

        <a href="https://t.me/username" class="btn telegram">
            <i class="fab fa-telegram"></i> <span>Telegram</span> <div></div>
        </a>

        <a href="https://instagram.com/username" class="btn instagram">
            <i class="fab fa-instagram"></i> <span>Instagram</span> <div></div>
        </a>

        <a href="https://snapchat.com/add/username" class="btn snapchat">
            <i class="fab fa-snapchat"></i> <span>Snapchat</span> <div></div>
        </a>

        <a href="viber://chat?number=964750xxxxxxx" class="btn viber">
            <i class="fab fa-viber"></i> <span>Viber</span> <div></div>
        </a>

        <a href="https://facebook.com/username" class="btn facebook">
            <i class="fab fa-facebook"></i> <span>Facebook</span> <div></div>
        </a>
    </div>

    <footer>Powered by Razwan Business</footer>

</body>
</html>
